
#include <iostream>

int main() {
    int a, b;
    // Read two integers from input
    std::cin >> a >> b;
    // Calculate the sum
    int sum = a + b;
    // Output the result
    std::cout << sum << std::endl;
    return 0;
}
